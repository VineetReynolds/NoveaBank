apiVersion: v1
kind: Service
metadata:
  name: keycloak-http
  labels:
    app: noveabank
spec:
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30880
  type: NodePort
  selector:
    app: noveabank